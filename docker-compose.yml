services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sensocrypt
      POSTGRES_PASSWORD: sensocrypt
      POSTGRES_DB: sensocrypt
    ports:
      - "5434:5432"  # distinct from the original SensoCrypt's 5433, so both can run at once
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sensocrypt"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: ./backend
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://sensocrypt:sensocrypt@postgres:5432/sensocrypt
    ports:
      - "8001:8000"  # distinct from the original SensoCrypt's 8000
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend/app:/app/app

volumes:
  postgres_data:
